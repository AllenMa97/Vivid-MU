# 启动脚本说明

本项目提供了多种便捷的启动方式，以满足不同用户的需求。

## Windows系统

### 1. Windows-命令行启动.bat
- **用途**: 以命令行模式启动项目
- **特点**: 显示详细处理过程，适合需要查看日志的用户
- **自动功能**:
  - 自动检测并使用内置Python环境
  - 自动配置FFmpeg路径
  - 自动下载缺失的模型文件
  - 自动运行完整的视频处理流程

### 2. Windows-GUI启动.bat
- **用途**: 以图形界面模式启动项目
- **特点**: 提供可视化界面，适合不熟悉命令行的用户
- **自动功能**:
  - 自动启动GUI界面
  - 提供直观的操作按钮

## macOS/Linux系统

### 1. Mac-Linux启动.sh
- **用途**: 以命令行模式启动项目
- **特点**: 显示详细处理过程，适合需要查看日志的用户
- **自动功能**:
  - 自动检测操作系统类型(macOS/Linux)
  - 自动使用对应系统的FFmpeg版本
  - 自动检测并使用内置Python环境
  - 自动下载缺失的模型文件
  - 自动运行完整的视频处理流程

### 2. Mac-Linux-GUI启动.sh
- **用途**: 以图形界面模式启动项目
- **特点**: 提供可视化界面，适合不熟悉命令行的用户
- **自动功能**:
  - 自动启动GUI界面
  - 提供直观的操作按钮

## 手动启动

如果需要手动启动，可以按照以下步骤操作：

1. 确保已安装Python 3.8+
2. 安装依赖包：
   ```bash
   pip install -r requirements.txt
   ```
3. 运行主程序：
   ```bash
   python main.py
   ```
4. 或运行GUI界面：
   ```bash
   python gui.py
   ```

## 注意事项

- 所有启动脚本都会自动使用项目内置的Python环境和FFmpeg工具
- 如果是首次运行，脚本会自动下载必要的模型文件
- 确保有足够的磁盘空间存储处理结果
- 处理大型视频文件时可能需要较长时间，请耐心等待