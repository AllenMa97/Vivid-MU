# Core dependencies (required)
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
imagehash>=4.3.1
librosa>=0.10.0
soundfile>=0.12.0

# Step 2 dependencies (for API calls)
httpx>=0.25.0

# 神经网络特征层 (P1/P2): YOLO 目标检测 / CLIP / Silero VAD 推理
onnxruntime>=1.16.0

# Optional dependencies (for advanced features)
# Uncomment if you want to use OpenVINO to run models on Intel CPU/iGPU/NPU
# openvino>=2024.0.0
# Uncomment if you want to use PyTorch-based features
# torch>=2.0.0
