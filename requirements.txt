# Core dependencies (required)
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
imagehash>=4.3.1
librosa>=0.10.0
soundfile>=0.12.0

# Step 2 dependencies (for API calls)
httpx>=0.25.0

# Optional dependencies (for advanced features)
# Uncomment if you want to use CLIP model for scene classification
# onnxruntime>=1.16.0
# Uncomment if you want to use VAD (Voice Activity Detection)
# torch>=2.0.0
